% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pricing.R
\name{mc_price_barrier}
\alias{mc_price_barrier}
\title{Knock-in / knock-out options pricing via Monte Carlo simulation}
\usage{
mc_price_barrier(
  S,
  K,
  T,
  r,
  sigma,
  N_paths,
  N_steps,
  B,
  is_upper,
  is_knockout,
  is_call
)
}
\arguments{
\item{S}{numeric scalar: current price of the underlying asset}

\item{K}{numeric scalar: strike price of the option}

\item{T}{numeric scalar: time to expiration of the option (in years)}

\item{r}{numeric scalar: risk-free interest rate (annualized)}

\item{sigma}{numeric scalar: volatility of the underlying asset (annualized)}

\item{N_paths}{integer scalar: number of simulated paths}

\item{N_steps}{integer scalar: number of time steps per path}

\item{B}{numeric scalar: barrier level}

\item{is_upper}{integer scalar: if 1, active when S >= B; if 0, active when S <= B}

\item{is_knockout}{integer scalar: if 1, barrier deactivates option; if 0, barrier activates option}

\item{is_call}{integer scalar: if 1, price a call option; if 0, price a put option}
}
\value{
Returns a numeric scalar in the range (0, +inf) representing the price of the option.
}
\description{
Computes the theoretical price of a knock-in / knock-out option using Monte Carlo with Geometric Brownian Motion paths.
}
\examples{
mc_price_barrier(100, 100, 1, 0.05, 0.2, 10000, 252, 120, 1, 1, 1)
stopifnot(mc_price_barrier(100, 100, 1, 0.05, 0.2, 1000, 52, 120, 1, 1, 1) < mc_price_european(100, 100, 1, 0.05, 0.2, 1000, 1)[1])
}
